package slokam.hospital.unittest;

import java.util.Date;
/*import java.util.List;

import org.junit.Assert;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;
import org.springframework.context.annotation.ComponentScan;
import org.springframework.test.context.junit4.SpringRunner;

import slokam.hospital.Controller.PatientController;
//import slokam.hospital.entity.Patient;
*/
/*@RunWith(SpringRunner.class)
@DataJpaTest
@ComponentScan("slokam.hospital.Controller,slokam.hospital.Service,slokam.hospital.daos")
*/public class GetPatientUnitTest {


	/*@Autowired
	PatientController ps;
	
	@org.junit.Test
	public void Test(){
		
      Patient p = new Patient();
      p.setName("prasad");
      p.setPhone(9738264898L);
      p.setDate(new Date());
      ps.savepatientdata(p);
      
      
      List<Patient>list= ps.getallPatient();
      Assert.assertEquals(1, list.size());
      System.out.println(list.size());
      System.out.println(list.get(0).getName());
      System.out.println(list.get(0).getPhone());
      System.out.println(list.get(0).getDate());
      
      Assert.assertEquals(p.getName(),list.get(0).getName());
      Assert.assertEquals(p.getPhone(), list.get(1).getPhone());
      
      
	}*/
}
